# Base URL of the FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google OAuth client ID (used for Google Sign-In button)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# NOTE: OCR / WhatsApp / Email provider keys are configured on the BACKEND,
# not here. See backend/.env.example. Those integrations are stubbed for now.
